---
interface Props {
  class?: string;
  tag?: string;
  padded?: boolean;
}

const { class: className = '', tag = 'div', padded = true } = Astro.props;
const Tag = tag as any;
---

<Tag class={`bento-card ${padded ? 'p-5' : ''} ${className}`}>
  <slot />
</Tag>
